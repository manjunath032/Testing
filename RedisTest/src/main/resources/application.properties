# Redis connection (used for local/dev)
spring.redis.host=localhost
spring.redis.port=6379

# Server
server.port=8080

# Logging
logging.level.org.springframework=INFO
logging.level.org.example=DEBUG

